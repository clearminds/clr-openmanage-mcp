# openmanage-mcp-server

Dell OpenManage Enterprise server management

## Install

```bash
pip install openmanage-mcp-server
# or
uvx openmanage-mcp-server
```

## Configuration

Configuration via environment variables. See `--help` for all options:

```bash
openmanage-mcp-server --help
```

## Development

```bash
git clone https://github.com/clearminds/openmanage-mcp-server.git
cd openmanage-mcp-server
uv sync
uv run openmanage-mcp-server
```

## License

MIT
